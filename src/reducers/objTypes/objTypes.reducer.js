import * as constants from './objTypes.constants'
import Model from './objTypes.model'

export default function objTypes (state = Model, { type, payload }) {
  switch (type) {
    default:
      return state
  }
}
